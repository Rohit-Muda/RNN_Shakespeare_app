# Shakespeare RNN App

Run locally with:

```bash
cd shakespeare_app
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run app.py
```

For Streamlit Cloud, this project uses Python 3.11 and pinned dependency versions to avoid install failures.
